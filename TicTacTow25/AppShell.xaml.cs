using TicTacTow25.Models;
using TicTacTow25.Views;

namespace TicTacTow25
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
           // Routing.RegisterRoute(Strings.MPGamesPageKey, typeof(MPGamesPage));
           // Routing.RegisterRoute(Strings.MainPageKey, typeof(MainPage));
        }
    }
}

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TicTacTow25.Views.GamePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:TicTacTow25.ViewModels"
    Title="GamePage"
    x:DataType="vm:GamePageVM">
    <VerticalStackLayout>
        <Label HorizontalOptions="Center" Text="{Binding OpponentName}" />
        <Label
            Margin="50"
            HorizontalOptions="Center"
            Text="{Binding StatusMessage}" />
        <Grid
            x:Name="grdBoard"
            ColumnSpacing="3"
            HorizontalOptions="Center"
            RowSpacing="3" />
        <Label HorizontalOptions="Center" Text="{Binding MyName}" />
    </VerticalStackLayout>
</ContentPage>